class Create<%= @class.name %> < ActiveRecord::Migration
  def change
    create_table :<%= table_name %> do |t|
      <% properties_columns.each do |line| %><%= line %>
      <% end %><% belongs_to_columns.each do |line| %><%= line %><% end %>
    end
  end
end
