<h1>Create a new Pet</h1>
<form action="/pets" method="POST">
  <p><label>Name: </label>
  <input type="text" id="pet_name" name="pet[name]"></p>

  <p>Choose existing owner(s):
  <% @owners.each do |owner| %>
    <input type="checkbox" name="pet[owner_id]" value="<%= owner.id %>" id="<%= owner.id %>"><%= owner.name %>
  <% end %></p>

  <p>and/or create a new owner:</p>
  <p><label>Name: </label><input type="text" id="owner_name" name="owner_name"></p>
  <p><input type="submit" value="Create Pet"></p>
</form>